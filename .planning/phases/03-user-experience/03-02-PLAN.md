---
phase: 03-user-experience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/stocks-table.tsx
  - src/components/holders-table.tsx
  - src/app/stocks/page.tsx
  - src/app/stocks/[code]/page.tsx
autonomous: true
requirements:
  - UX-02
user_setup: []

must_haves:
  truths:
    - "User sees skeleton screens for tables during data fetch"
    - "User sees loading spinner for page transitions"
    - "User sees appropriate error states with retry buttons"
    - "User sees empty states when no data is available"
    - "All loading states provide visual feedback about what's happening"
  artifacts:
    - path: "src/components/stocks-table.tsx"
      provides: "Stock table with skeleton loading state"
      contains: "isLoading, skeleton rows"
    - path: "src/components/holders-table.tsx"
      provides: "Holders table with skeleton loading state"
      contains: "isLoading, skeleton rows"
    - path: "src/app/stocks/page.tsx"
      provides: "Stock listing page with error and empty states"
      contains: "error, retry, empty state components"
    - path: "src/app/stocks/[code]/page.tsx"
      provides: "Stock detail page with error and empty states"
      contains: "error, retry, empty state components"
  key_links:
    - from: "src/components/stocks-table.tsx"
      to: "loading state"
      via: "isLoading prop or Suspense fallback"
      pattern: "isLoading|skeleton|fallback"
    - from: "src/app/stocks/page.tsx"
      to: "error boundary"
      via: "try/catch in async component or error.tsx"
      pattern: "error|retry"
---

<objective>
Implement comprehensive loading states, error handling, and empty states to provide clear visual feedback during all data operations.

**Purpose**: Users should never wonder if the app is working. Skeleton screens show content structure during fetch, error states explain what went wrong with retry options, and empty states guide users when no data exists.

**Output**: Professional loading experience with skeleton screens, meaningful error messages with retry functionality, and helpful empty states across all pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-visualization/02-CONTEXT.md

@src/components/stocks-table.tsx
@src/components/holders-table.tsx
@src/app/stocks/page.tsx
@src/app/stocks/[code]/page.tsx
</context>

<interfaces>
<!-- Existing components to be enhanced with loading/error states -->

From src/components/stocks-table.tsx:
```typescript
export function StocksTable({ data }: StocksTableProps);
// Current: renders data directly
// Enhancement: add isLoading prop, render skeleton rows when true
```

From src/components/holders-table.tsx:
```typescript
export function HoldersTable({ data, onExport }: HoldersTableProps);
// Current: renders data directly
// Enhancement: add isLoading prop, render skeleton rows when true
```

From src/app/stocks/page.tsx:
```typescript
export default function StocksPage();
// Current: basic Suspense with spinner
// Enhancement: add error boundary, better empty state, skeleton
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add skeleton loading to StocksTable component</name>
  <files>src/components/stocks-table.tsx</files>
  <action>
    Enhance StocksTable with skeleton loading state:

    1. **Add isLoading prop**:
    ```tsx
    interface StocksTableProps {
      data: StockData[];
      isLoading?: boolean;
    }
    ```

    2. **Create SkeletonRow component**:
    ```tsx
    function SkeletonRow() {
      return (
        <tr className="animate-pulse">
          <td className="px-2 sm:px-4 md:px-6 py-2 sm:py-3 md:py-4">
            <div className="h-4 bg-gray-200 rounded w-16"></div>
          </td>
          <td className="px-2 sm:px-4 md:px-6 py-2 sm:py-3 md:py-4">
            <div className="h-4 bg-gray-200 rounded w-32"></div>
          </td>
          <td className="px-2 sm:px-4 md:px-6 py-2 sm:py-3 md:py-4">
            <div className="h-4 bg-gray-200 rounded w-24"></div>
          </td>
          <td className="px-2 sm:px-4 md:px-6 py-2 sm:py-3 md:py-4">
            <div className="h-4 bg-gray-200 rounded w-12"></div>
          </td>
          <td className="px-2 sm:px-4 md:px-6 py-2 sm:py-3 md:py-4">
            <div className="h-4 bg-gray-200 rounded w-20"></div>
          </td>
        </tr>
      );
    }
    ```

    3. **Render skeleton when loading**:
    ```tsx
    export function StocksTable({ data, isLoading = false }: StocksTableProps) {
      const table = useReactTable({
        data,
        columns,
        getCoreRowModel: getCoreRowModel(),
      });

      if (isLoading) {
        return (
          <div className="overflow-x-auto max-w-full">
            <table className="min-w-full bg-white border border-gray-200">
              <thead className="bg-gray-50">
                {/* Render header normally */}
                {table.getHeaderGroups().map((headerGroup) => (
                  <tr key={headerGroup.id}>
                    {headerGroup.headers.map((header) => (
                      <th
                        key={header.id}
                        className="px-2 sm:px-4 md:px-6 py-2 sm:py-3 md:py-4 text-left text-[10px] sm:text-xs font-medium text-gray-500 uppercase tracking-wider"
                      >
                        {header.isPlaceholder
                          ? null
                          : flexRender(header.column.columnDef.header, header.getContext())
                        }
                      </th>
                    ))}
                  </tr>
                ))}
              </thead>
              <tbody className="bg-white divide-y divide-gray-200">
                {/* Render 10 skeleton rows */}
                {Array.from({ length: 10 }).map((_, i) => (
                  <SkeletonRow key={i} />
                ))}
              </tbody>
            </table>
          </div>
        );
      }

      // Existing table rendering...
    }
    ```

    4. **Preserve mobile responsiveness**:
       - Keep all sticky column classes
       - Keep responsive padding (px-2 sm:px-4 md:px-6)
       - Keep responsive text sizes

    Notes:
    - Skeleton rows match the exact structure of real rows
    - 10 rows provides good visual feedback (common table size)
    - Animate-pulse creates subtle shimmer effect
    - Gray-200 matches Tailwind's default skeleton color
  </action>
  <verify>
    <automated>grep -E "SkeletonRow|isLoading" src/components/stocks-table.tsx</automated>
  </verify>
  <done>StocksTable renders skeleton rows when isLoading=true, matches table structure exactly</done>
</task>

<task type="auto">
  <name>Task 2: Add skeleton loading to HoldersTable component</name>
  <files>src/components/holders-table.tsx</files>
  <action>
    Apply the same skeleton loading pattern to HoldersTable:

    1. **Add isLoading prop**:
    ```tsx
    interface HoldersTableProps {
      data: HolderData[];
      onExport?: () => void;
      isLoading?: boolean;
    }
    ```

    2. **Create SkeletonRow for holders** (5 columns: rank, name, shares, percentage, change):
    ```tsx
    function SkeletonRow() {
      return (
        <tr className="animate-pulse">
          <td className="px-2 sm:px-4 md:px-6 py-2 sm:py-3 md:py-4">
            <div className="h-4 bg-gray-200 rounded w-8"></div>
          </td>
          <td className="px-2 sm:px-4 md:px-6 py-2 sm:py-3 md:py-4">
            <div className="h-4 bg-gray-200 rounded w-48"></div>
          </td>
          <td className="px-2 sm:px-4 md:px-6 py-2 sm:py-3 md:py-4">
            <div className="h-4 bg-gray-200 rounded w-32"></div>
          </td>
          <td className="px-2 sm:px-4 md:px-6 py-2 sm:py-3 md:py-4">
            <div className="h-4 bg-gray-200 rounded w-16"></div>
          </td>
          <td className="px-2 sm:px-4 md:px-6 py-2 sm:py-3 md:py-4">
            <div className="h-4 bg-gray-200 rounded w-20"></div>
          </td>
        </tr>
      );
    }
    ```

    3. **Render skeleton when loading**:
       - Same pattern as StocksTable
       - Render 10 skeleton rows
       - Keep header visible (shows structure)

    4. **Preserve existing functionality**:
       - Keep sorting, export button
       - Keep mobile responsiveness (sticky first column)
       - Keep responsive padding

    Notes:
    - Holder names are longer, so skeleton width is w-48 vs w-32 for stocks
  </action>
  <verify>
    <automated>grep -E "SkeletonRow|isLoading" src/components/holders-table.tsx</automated>
  </verify>
  <done>HoldersTable renders skeleton rows when isLoading=true, matches table structure</done>
</task>

<task type="auto">
  <name>Task 3: Add error and empty states to stock listing page</name>
  <files>src/app/stocks/page.tsx</files>
  <action>
    Enhance stock listing page with error handling and improved empty states:

    1. **Create ErrorState component**:
    ```tsx
    function ErrorState({ error, onRetry }: { error: string; onRetry: () => void }) {
      return (
        <div className="text-center py-12">
          <div className="inline-flex items-center justify-center w-16 h-16 rounded-full bg-red-100 mb-4">
            <svg className="w-8 h-8 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
            </svg>
          </div>
          <h3 className="text-lg font-semibold text-gray-900 mb-2">
            Terjadi Kesalahan
          </h3>
          <p className="text-gray-600 mb-4">{error}</p>
          <button
            onClick={onRetry}
            className="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 min-h-12"
          >
            <svg className="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />
            </svg>
            Coba Lagi
          </button>
        </div>
      );
    }
    ```

    2. **Enhance EmptyState component**:
    ```tsx
    function EmptyState() {
      return (
        <div className="text-center py-12">
          <div className="inline-flex items-center justify-center w-16 h-16 rounded-full bg-gray-100 mb-4">
            <svg className="w-8 h-8 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M20 13V6a2 2 0 00-2-2H6a2 2 0 00-2 2v7m16 0v5a2 2 0 01-2 2H6a2 2 0 01-2-2v-5m16 0h-2.586a1 1 0 00-.707.293l-2.414 2.414a1 1 0 01-.707.293h-3.172a1 1 0 01-.707-.293l-2.414-2.414A1 1 0 006.586 13H4" />
            </svg>
          </div>
          <h3 className="text-lg font-semibold text-gray-900 mb-2">
            Belum Ada Data
          </h3>
          <p className="text-gray-600 max-w-md mx-auto">
            Data kepemilikan saham belum tersedia. Data akan muncul setelah file PDF dari IDX diimpor ke sistem.
          </p>
        </div>
      );
    }
    ```

    3. **Create NoResultsState for search**:
    ```tsx
    function NoResultsState({ query }: { query: string }) {
      return (
        <div className="text-center py-12">
          <div className="inline-flex items-center justify-center w-16 h-16 rounded-full bg-yellow-100 mb-4">
            <svg className="w-8 h-8 text-yellow-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
            </svg>
          </div>
          <h3 className="text-lg font-semibold text-gray-900 mb-2">
            Tidak Ditemukan
          </h3>
          <p className="text-gray-600">
            Tidak ada saham yang cocok dengan pencarian "{query}"
          </p>
          <p className="text-gray-500 text-sm mt-2">
            Coba cari dengan kata kunci lain
          </p>
        </div>
      );
    }
    ```

    4. **Update StocksList component** with error handling:
    ```tsx
    'use client';

    import { useState } from 'react';

    async function StocksList({ searchParams }: { searchParams?: { q?: string; holder?: string } }) {
      const [error, setError] = useState<string | null>(null);

      try {
        const stocks = await findAllStocksWithFilters(searchParams);
        const latestPeriod = await getLatestPeriod();

        if (stocks.length === 0) {
          // Differentiate between no data at all vs no search results
          if (searchParams?.q || searchParams?.holder) {
            return <NoResultsState query={searchParams.q || searchParams.holder || ''} />;
          }
          return <EmptyState />;
        }

        // Normal rendering...
      } catch (err) {
        setError(err instanceof Error ? err.message : 'Unknown error');
      }

      if (error) {
        return <ErrorState error={error} onRetry={() => window.location.reload()} />;
      }
    }
    ```

    5. **Update Suspense fallback** to use skeleton:
    ```tsx
    <Suspense fallback={<StocksTable data={[]} isLoading={true} />}>
      <StocksList searchParams={searchParams} />
    </Suspense>
    ```

    Notes:
    - All text in Indonesian
    - Touch-friendly retry button (min-h-12)
    - Clear visual distinction between empty, no results, and error states
    - Meaningful icons for each state
  </action>
  <verify>
    <automated>grep -E "ErrorState|EmptyState|NoResultsState" src/app/stocks/page.tsx</automated>
  </verify>
  <done>Stock listing page shows skeleton during load, meaningful error/empty/no-results states with retry</done>
</task>

<task type="auto">
  <name>Task 4: Add error and empty states to stock detail page</name>
  <files>src/app/stocks/[code]/page.tsx</files>
  <action>
    Apply error handling and empty states to stock detail page:

    1. **Reuse ErrorState and EmptyState** components (copy from stocks/page.tsx or create shared)

    2. **Add NotFoundState for invalid stock codes**:
    ```tsx
    function NotFoundState({ code }: { code: string }) {
      return (
        <div className="text-center py-12">
          <div className="inline-flex items-center justify-center w-16 h-16 rounded-full bg-gray-100 mb-4">
            <svg className="w-8 h-8 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
          <h3 className="text-lg font-semibold text-gray-900 mb-2">
            Saham Tidak Ditemukan
          </h3>
          <p className="text-gray-600 mb-4">
            Kode saham "{code}" tidak ditemukan dalam database.
          </p>
          <Link
            href="/stocks"
            className="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 min-h-12"
          >
            <svg className="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M10 19l-7-7m0 0l7-7m-7 7h18" />
            </svg>
            Kembali ke Daftar Saham
          </Link>
        </div>
      );
    }
    ```

    3. **Update page component** with error handling:
    ```tsx
    export default async function StockDetailPage({ params }: { params: { code: string } }) {
      try {
        const [stock, ownership] = await Promise.all([
          findEmitenByCode(params.code),
          findOwnershipByStockWithHolders(params.code)
        ]);

        if (!stock) {
          return <NotFoundState code={params.code} />;
        }

        if (ownership.length === 0) {
          return (
            <div>
              {/* Stock header */}
              <EmptyState />
            </div>
          );
        }

        // Normal rendering...
      } catch (err) {
        return <ErrorState error={err.message} onRetry={() => window.location.reload()} />;
      }
    }
    ```

    4. **Update Suspense fallback** to use skeleton:
    ```tsx
    <Suspense fallback={
      <div className="container mx-auto px-4 py-8">
        <div className="animate-pulse">
          <div className="h-8 bg-gray-200 rounded w-48 mb-4"></div>
          <div className="h-4 bg-gray-200 rounded w-64 mb-8"></div>
          <HoldersTable data={[]} isLoading={true} />
        </div>
      </div>
    }>
      {/* Content */}
    </Suspense>
    ```

    Notes:
    - notFound() is already used for 404s, but NotFoundState provides better UX
    - Can keep notFound() for SEO, but render NotFoundState in error.tsx
  </action>
  <verify>
    <automated>grep -E "ErrorState|NotFoundState" src/app/stocks/[code]/page.tsx</automated>
  </verify>
  <done>Stock detail page shows skeleton during load, handles notFound/error/empty states with clear messaging</done>
</task>

</tasks>

<verification>
1. Start dev server: npm run dev
2. Test loading states:
   - Visit /stocks and observe skeleton animation during initial load
   - Navigate to /stocks/TLKM (or any valid stock) and see skeleton
   - Verify skeleton rows match real row structure exactly
3. Test error states (simulate error):
   - Temporarily break database query to trigger error
   - Verify error message appears with retry button
   - Click retry and verify it reloads the page
4. Test empty states:
   - Clear database data or visit before import
   - Verify "Belum Ada Data" message appears
   - Verify message explains what to do
5. Test no-results state:
   - Search for non-existent stock: "XYZ123"
   - Verify "Tidak Ditemukan" message appears
   - Verify search query is shown in message
6. Test notFound state:
   - Visit /stocks/INVALIDCODE
   - Verify "Saham Tidak Ditemukan" message appears
   - Verify "Kembali ke Daftar Saham" button works
7. Test all states on mobile (320px):
   - Verify all states are readable and touch-friendly
   - Verify buttons have minimum 44px height
</verification>

<success_criteria>
1. Skeleton screens show for all tables during data fetch (10 rows)
2. Skeleton rows match exact structure of real data rows
3. Error states display meaningful error messages in Indonesian
4. Retry button appears in error states and successfully reloads
5. Empty state appears when no data exists with helpful explanation
6. No-results state appears for failed searches with query shown
7. NotFound state appears for invalid stock codes with back button
8. All states use appropriate icons and visual hierarchy
9. All interactive elements are touch-friendly (44px min)
10. Loading spinner appears for page transitions (Suspense fallback)
</success_criteria>

<output>
After completion, create `.planning/phases/03-user-experience/03-02-SUMMARY.md`
</output>
