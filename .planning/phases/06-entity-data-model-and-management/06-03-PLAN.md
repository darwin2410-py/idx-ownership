---
phase: 06-entity-data-model-and-management
plan: "03"
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - src/app/entities/page.tsx
  - src/app/entities/[id]/page.tsx
  - src/components/entity-create-form.tsx
  - src/components/entity-aliases-table.tsx
  - src/components/alias-search-combobox.tsx
  - src/app/layout.tsx
autonomous: true
requirements:
  - ENTITY-01
  - ENTITY-02
  - ENTITY-03

must_haves:
  truths:
    - "User can visit /entities and see a table of all entities with name, alias count, and link to detail page"
    - "User can fill the inline form above the entity table and create a new entity — it appears in the list after submit"
    - "User receives an inline error when submitting a duplicate entity name"
    - "User can visit /entities/[id] and see the entity name and a list of its aliases (holder name + delete button per row)"
    - "User can click Tambah Alias, type a partial name in the combobox, see ranked results, and select one to add it"
    - "User receives a clear inline error message when adding a holder that already belongs to another entity"
    - "User can delete an alias and the alias disappears from the list without a page reload failure"
    - "Navigation includes an Entities link that routes to /entities"
  artifacts:
    - path: "src/app/entities/page.tsx"
      provides: "Entity list page at /entities"
      contains: "EntityCreateForm"
    - path: "src/app/entities/[id]/page.tsx"
      provides: "Entity detail page at /entities/[id]"
      contains: "EntityAliasesTable"
    - path: "src/components/entity-create-form.tsx"
      provides: "Client component — inline create form"
      contains: "use client"
    - path: "src/components/entity-aliases-table.tsx"
      provides: "Client component — alias list with delete buttons and combobox trigger"
      contains: "use client"
    - path: "src/components/alias-search-combobox.tsx"
      provides: "Client component — cmdk Command.Dialog for fuzzy holder search"
      contains: "Command"
    - path: "src/app/layout.tsx"
      provides: "Nav link to /entities"
      contains: "/entities"
  key_links:
    - from: "EntityCreateForm.handleSubmit"
      to: "POST /api/entities"
      via: "fetch + router.refresh()"
      pattern: "fetch.*api/entities"
    - from: "EntityAliasesTable delete button"
      to: "DELETE /api/entities/[id]/aliases/[holderId]"
      via: "fetch + router.refresh()"
      pattern: "DELETE.*aliases"
    - from: "AliasSearchCombobox search input"
      to: "GET /api/holders/search?q="
      via: "300ms debounced fetch"
      pattern: "holders/search"
    - from: "AliasSearchCombobox handleSelect"
      to: "POST /api/entities/[id]/aliases"
      via: "fetch, 409 → setError(data.message)"
      pattern: "POST.*aliases"
---

<objective>
Build the entity list page, entity detail page, and three client components: inline create form, alias table with delete, and the cmdk fuzzy search combobox.

Purpose: Delivers the full user-facing CRUD for entity grouping. Users can create entities, link holders as aliases, detect and handle conflicts, and unlink aliases — all through a consistent UI matching existing /holders and /stocks page patterns.
Output: /entities and /entities/[id] pages fully functional; nav link added; cmdk combobox integrated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-entity-data-model-and-management/06-CONTEXT.md
@.planning/phases/06-entity-data-model-and-management/06-RESEARCH.md
@.planning/phases/06-entity-data-model-and-management/06-01-SUMMARY.md
@.planning/phases/06-entity-data-model-and-management/06-02-SUMMARY.md

<interfaces>
<!-- Entity repository functions (from Plan 02): -->
```typescript
// src/lib/repositories/entity-repository.ts
export async function findAllEntities(): Promise<Array<{
  id: number; name: string; description: string | null; aliasCount: number; createdAt: Date;
}>>

export async function findEntityById(id: number): Promise<Entity | null>

export async function findAliasesByEntityId(entityId: number): Promise<Array<{
  holderId: number; holderName: string; holderType: string; addedAt: Date;
}>>
```

<!-- API routes (from Plan 02): -->
```
POST   /api/entities                          → { name, description? }   → 201 | 409
POST   /api/entities/[id]/aliases             → { holderId }             → 200 | 409 { ownerEntityName }
DELETE /api/entities/[id]/aliases/[holderId]                             → 200
GET    /api/holders/search?q=                                            → [{ id, name, type }]
```

<!-- cmdk usage pattern (confirmed from docs): -->
```tsx
import { Command } from 'cmdk';
// <Command.Dialog open={open} onOpenChange={setOpen}>
//   <Command.Input value={search} onValueChange={setSearch} />
//   <Command.List>
//     <Command.Item key={id} onSelect={() => handleSelect(id)}>{name}</Command.Item>
//   </Command.List>
// </Command.Dialog>
```

<!-- Existing page patterns to match: -->
<!-- src/app/holders/page.tsx — list page with search above table -->
<!-- src/app/layout.tsx — nav links pattern (see current links in file) -->
<!-- Established: Server Components for data fetch, Client Components for interactivity -->
<!-- Established: router.refresh() after mutation to revalidate server component data -->
<!-- Established: export const revalidate = 0 on pages with mutations -->
<!-- Established: Indonesian UI labels -->
<!-- Established: min-h-12 on all buttons (48px touch target, KD-012) -->
<!-- Established: container mx-auto px-4 py-8 page layout -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entity list page (/entities) with inline create form</name>
  <files>
    src/app/entities/page.tsx,
    src/components/entity-create-form.tsx
  </files>
  <action>
**`src/app/entities/page.tsx`** — Server Component:
- `export const revalidate = 0` (no cache — form creates entities inline)
- Fetch `findAllEntities()` server-side
- Render: page heading "Daftar Entitas", `<EntityCreateForm />` above the table, then a simple table
- Table columns: Nama Entitas | Jumlah Alias | Aksi
- Each row: entity.name as clickable link to `/entities/${entity.id}` | entity.aliasCount | "Lihat" link
- Empty state: "Belum ada entitas. Buat entitas pertama di atas." (shown when entities.length === 0)
- Use same `container mx-auto px-4 py-8` layout as /holders/page.tsx

**`src/components/entity-create-form.tsx`** — Client Component ('use client'):
- Form fields: "Nama Entitas" (text input, required, max 100 chars) + optional "Deskripsi" (textarea, max 500 chars)
- Submit button: "Buat Entitas" (min-h-12 per KD-012)
- On submit: POST /api/entities with { name, description }
- On 201: clear form + `router.refresh()` to refresh the server component entity list
- On 409: show inline error "Nama entitas sudah digunakan" below the name input
- On network error: show "Terjadi kesalahan, coba lagi"
- Loading state: disable button + show "Menyimpan..." text while request in flight
- Description field: Claude's discretion — include it (it was noted as optional in CONTEXT.md)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>/entities page renders entity list table; EntityCreateForm submits to API; 409 shows inline error; 201 triggers list refresh</done>
</task>

<task type="auto">
  <name>Task 2: Entity detail page (/entities/[id]) with alias table and cmdk combobox + nav link</name>
  <files>
    src/app/entities/[id]/page.tsx,
    src/components/entity-aliases-table.tsx,
    src/components/alias-search-combobox.tsx,
    src/app/layout.tsx
  </files>
  <action>
**`src/app/entities/[id]/page.tsx`** — Server Component:
- `export const revalidate = 0`
- Parse id from params (parseInt), 404 if entity not found via `findEntityById(id)`
- Fetch aliases via `findAliasesByEntityId(id)`
- Render: entity name as heading, `<EntityAliasesTable entityId={id} aliases={aliases} />`
- If entity not found: `import { notFound } from 'next/navigation'; notFound()`

**`src/components/entity-aliases-table.tsx`** — Client Component ('use client'):
- Props: `{ entityId: number; aliases: Array<{ holderId, holderName, holderType, addedAt }> }`
- Renders: "Tambah Alias" button (min-h-12) that opens the AliasSearchCombobox
- Renders: table of aliases with columns: Nama Holder | Tipe | Aksi
- Per alias row: holderName | holderType (localized: 'individual'→'Individu', 'institution'→'Institusi', 'foreign'→'Asing', 'government'→'Pemerintah') | delete button "Hapus" (min-h-12)
- Delete button: calls DELETE /api/entities/[entityId]/aliases/[holderId], then `router.refresh()`
- Empty state (no aliases): "Belum ada alias. Tambah holder dengan tombol di atas."
- Inline error display for delete failures
- Includes `<AliasSearchCombobox entityId={entityId} />` with open state controlled by the "Tambah Alias" button

**`src/components/alias-search-combobox.tsx`** — Client Component ('use client'):
- Props: `{ entityId: number; open: boolean; onOpenChange: (open: boolean) => void }`
- Uses `import { Command } from 'cmdk'`
- Input placeholder: "Cari nama holder..." with 300ms debounce
- Minimum 2 chars to trigger search (shows "Ketik minimal 2 karakter" hint below 2 chars)
- Fetches GET /api/holders/search?q={search} on debounce trigger
- Displays results as Command.Items: holder name + type badge
- On select: POST /api/entities/[entityId]/aliases with { holderId }
  - On 200: close combobox + `router.refresh()`
  - On 409: show error message below input inside combobox: `data.message` (e.g., "Holder ini sudah terdaftar di entity \"Hartono Family\"")
  - On other error: show "Terjadi kesalahan, coba lagi"
- Style the dialog: white background, border, rounded, shadow — consistent with existing modal-like elements in codebase
- Loading indicator while fetching search results

**`src/app/layout.tsx`** — add Entities nav link:
- Add between the existing "Beranda" and "Daftar Saham" links:
  ```tsx
  <Link
    href="/entities"
    className="text-sm font-medium text-gray-700 hover:text-gray-900 transition-colors"
  >
    Entitas
  </Link>
  ```
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>/entities/[id] page renders entity name + alias table; "Tambah Alias" opens cmdk combobox; fuzzy search works; alias add shows 409 conflict message inline; delete removes alias; nav shows "Entitas" link; `npm run build` exits 0</done>
</task>

</tasks>

<verification>
Full user flow (manual smoke test after `npm run dev`):
1. Visit http://localhost:3000/entities — page loads, empty state visible, create form present
2. Create entity "Hartono Family" — appears in list with 0 aliases
3. Click "Lihat" → /entities/[id] — entity name shown, alias list empty
4. Click "Tambah Alias" → combobox opens
5. Type "robert" → ranked holders appear (min 2 chars required)
6. Select a holder → alias appears in list, combobox closes
7. Click "Tambah Alias" again, select the same holder → conflict error message shows inline: "Holder ini sudah terdaftar di entity..."
8. Click "Hapus" on alias row → alias disappears; revisit /entities shows aliasCount = 0
9. Navigate via "Entitas" nav link → routes to /entities correctly
10. `npm run build` exits 0
</verification>

<success_criteria>
- /entities: table with name + aliasCount + link; inline create form; empty state; 409 error on duplicate name
- /entities/[id]: entity name heading; alias table with holder name + type + delete button; "Tambah Alias" button
- AliasSearchCombobox: opens on button click; debounced search (300ms, min 2 chars); ranked results; 409 conflict shown inline
- Delete alias: removes join row only; page refreshes; ownership data intact
- Nav: "Entitas" link routes to /entities
- Build: `npm run build` exits 0 with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-entity-data-model-and-management/06-03-SUMMARY.md`
</output>
