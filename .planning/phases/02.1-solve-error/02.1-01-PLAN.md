---
phase: 02.1-solve-error
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.env.example, README.md, drizzle.config.ts]
autonomous: true
requirements: [ENV-01]
user_setup: []

must_haves:
  truths:
    - "Application starts without DATABASE_URL error"
    - "Developer can run database migrations successfully"
    - "Environment variable is documented for setup"
    - "Local development works with proper database connection"
  artifacts:
    - path: ".env.example"
      provides: "Template for required environment variables"
      contains: "DATABASE_URL"
    - path: "README.md"
      provides: "Setup instructions with environment configuration"
      contains: "Prerequisites", "Environment Setup", "Troubleshooting"
    - path: "drizzle.config.ts"
      provides: "Drizzle configuration with DATABASE_URL from env"
      contains: "process.env.DATABASE_URL"
  key_links:
    - from: "drizzle.config.ts"
      to: "process.env.DATABASE_URL"
      via: "environment variable"
      pattern: "DATABASE_URL"
    - from: ".env"
      to: "drizzle.config.ts"
      via: "dotenv loading (Next.js default)"
      pattern: "DATABASE_URL"
---

<objective>
Fix DATABASE_URL environment configuration to enable local development and database migrations.

Purpose: The application fails to start with "No database connection string was provided to `neon()`" error because DATABASE_URL is not configured. This plan creates the necessary environment file template and documentation to unblock development.

Output: Working local development environment with database connectivity
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02.1-solve-error/02.1-CONTEXT.md
@src/lib/db/index.ts
@package.json
@drizzle.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .env.example template</name>
  <files>.env.example</files>
  <action>
    Create .env.example file in project root with DATABASE_URL placeholder:

    ```
    # Database
    # Get connection string from Neon console: https://console.neon.tech/
    # Format: postgresql://[user]:[password]@[neon-host]/[dbname]?sslmode=require
    DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

    # Future environment variables (API keys, etc.) go here
    ```

    Note: .gitignore already contains .env* patterns (line 28), so .env will be excluded from version control.
  </action>
  <verify>
    <automated>test -f .env.example && grep -q "DATABASE_URL" .env.example</automated>
  </verify>
  <done>.env.example file exists with DATABASE_URL placeholder and Neon console link</done>
</task>

<task type="auto">
  <name>Task 2: Create README with setup instructions</name>
  <files>README.md</files>
  <action>
    Create README.md with environment setup section:

    1. Project title and brief description (IDX Ownership Visualizer - visualization tool for IDX stock ownership data)

    2. Prerequisites section:
       - Node.js 18+ (check package.json engines or specify based on Next.js 15 requirements)
       - Neon account (free tier available)
       - Git (for cloning)

    3. Quick Start section:
       ```bash
       # Install dependencies
       npm install

       # Create .env from example
       cp .env.example .env

       # Edit .env and add your DATABASE_URL from Neon console
       # Open https://console.neon.tech/ → copy connection string

       # Push database schema
       npm run db:push

       # Start development server
       npm run dev
       ```

    4. Environment Setup section:
       - Explain how to get DATABASE_URL from Neon console
       - Show connection string format
       - Note that same DATABASE_URL works for local and production

    5. Available Scripts section (from package.json):
       - `npm run dev` - Start development server
       - `npm run build` - Build for production
       - `npm run db:push` - Push schema changes to database
       - `npm run db:generate` - Generate migration files
       - `npm run import-pdf` - Import PDF data (for testing)

    6. Troubleshooting FAQ:
       - Q: "No database connection string was provided" error
       - A: Create .env file from .env.example and add DATABASE_URL from Neon console
       - Q: Database connection timeout
       - A: Verify DATABASE_URL has ?sslmode=require parameter
       - Q: drizzle-kit command not found
       - A: Run `npm install` to install devDependencies

    Keep it concise and code-focused. No lengthy prose - command examples with brief explanations.
  </action>
  <verify>
    <automated>test -f README.md && grep -q "DATABASE_URL" README.md && grep -q "Neon" README.md && grep -q "npm run dev" README.md</automated>
  </verify>
  <done>README.md exists with Prerequisites, Quick Start, Environment Setup, Scripts, and Troubleshooting sections</done>
</task>

<task type="auto">
  <name>Task 3: Verify drizzle.config.ts reads DATABASE_URL</name>
  <files>drizzle.config.ts</files>
  <action>
    Read drizzle.config.ts to verify it's configured to read DATABASE_URL from environment:

    Expected structure:
    ```typescript
    import { defineConfig } from 'drizzle-kit';

    export default defineConfig({
      schema: './src/lib/db/schema.ts',
      out: './drizzle',
      dialect: 'postgresql',
      dbCredentials: {
        url: process.env.DATABASE_URL || '',
      },
    });
    ```

    If file is missing or misconfigured, update it to match this structure. The key requirement is that it reads from process.env.DATABASE_URL.

    Note: Next.js automatically loads .env files via dotenv, so no explicit dotenv.config() is needed.
  </action>
  <verify>
    <automated>grep -q "process.env.DATABASE_URL" drizzle.config.ts</automated>
  </verify>
  <done>drizzle.config.ts reads DATABASE_URL from environment variables</done>
</task>

</tasks>

<verification>
Overall phase checks:
1. .env.example file exists with DATABASE_URL placeholder
2. README.md has complete setup instructions
3. drizzle.config.ts reads DATABASE_URL from process.env
4. Developer can follow README to set up local environment
5. Application can connect to database once DATABASE_URL is configured

Note: Actual database connection testing requires user to provide their Neon DATABASE_URL, which is a manual setup step. This plan ensures all configuration is in place for that setup to work.
</verification>

<success_criteria>
1. .env.example created and committed to repository
2. README.md created with comprehensive setup instructions
3. drizzle.config.ts verified to read DATABASE_URL from environment
4. Developer has clear path to set up local environment (follow README)
5. No code changes required - this is pure configuration and documentation
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-solve-error/02.1-01-SUMMARY.md`
</output>
