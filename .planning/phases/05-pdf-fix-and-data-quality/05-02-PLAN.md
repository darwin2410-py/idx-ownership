---
phase: 05-pdf-fix-and-data-quality
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified: []
autonomous: true
requirements:
  - PARSE-02

must_haves:
  truths:
    - "All data from before the fix is cleared — no truncated holder names remain as orphans"
    - "Re-import from ownership_one_percent_202603.pdf completes without errors"
    - "Database contains at least 7209 ownership records after re-import"
    - "Spot-check confirms 'Prajogo Pangestu' appears in full in the holders table"
    - "Stock codes, ownership percentages, and share counts are correct for spot-checked records"
  artifacts:
    - path: "scripts/clear-all-data.ts"
      provides: "Clears all data in FK-safe order (existing script, no changes)"
      contains: "DELETE FROM ownership_records"
    - path: "scripts/check-period.ts"
      provides: "Counts records per period after re-import (existing script, no changes)"
      contains: "SELECT"
    - path: "scripts/audit-holder-names.ts"
      provides: "Spot-check SQL to confirm full holder names after re-import"
      contains: "FROM holders"
  key_links:
    - from: "scripts/clear-all-data.ts"
      to: "Neon database"
      via: "DELETE in FK-safe order: ownership_records, holders, emiten, periods"
      pattern: "DELETE FROM"
    - from: "npm run import-pdf"
      to: "src/lib/services/import-service.ts"
      via: "scripts/manual-import.ts"
      pattern: "importFromPDF"
---

<objective>
Clear all existing database data and re-import using the fixed PDF extractor to produce clean, complete holder names throughout the database.

Purpose: The extractor fix in Plan 05-01 only affects future extractions. Existing data in the database still contains truncated names from the old (broken) extractor. This plan wipes and re-imports to make the database match the fixed extractor's output.

Output: A database containing only clean, untruncated holder names from a fresh import of `ownership_one_percent_202603.pdf` using the fixed extractor from Plan 05-01.

Note: Only one real period of data exists (2026-03). Synthetic historical seed data is also cleared. The result is a single clean period of real data.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pdf-fix-and-data-quality/05-RESEARCH.md
@.planning/phases/05-pdf-fix-and-data-quality/05-01-SUMMARY.md
</context>

<interfaces>
<!-- Existing scripts used in this plan. No code changes — orchestration only. -->

scripts/clear-all-data.ts (existing, no changes):
- Deletes all rows in FK-safe order: ownership_records → holders → emiten → periods
- Run: `npx tsx scripts/clear-all-data.ts`

scripts/check-period.ts (existing, no changes):
- Queries periods table and prints record counts per period
- Run: `npx tsx scripts/check-period.ts`

scripts/audit-holder-names.ts (created in Plan 05-01):
- Queries holders for short names and known-name spot-checks
- Run: `npx tsx scripts/audit-holder-names.ts`
- Exit code 0 = no truncations found (clean state)
- Exit code 1 = truncations still present

npm run import-pdf (existing, no changes):
- Defined in package.json, runs scripts/manual-import.ts
- Usage: `npm run import-pdf -- ./ownership_one_percent_202603.pdf`
- Calls importFromPDF() from import-service.ts with forceUpdate handled by --force flag
- Already handles full pipeline: extract → validate → upsert to DB
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Audit current state, clear all data, re-import</name>
  <files></files>
  <action>
Execute the re-import workflow in this exact sequence. Do not skip steps or reorder them.

**Step 1 — Audit current state (before clear):**
```bash
npx tsx scripts/check-period.ts
npx tsx scripts/audit-holder-names.ts
```
Log the output. This is the "before" snapshot for comparison.

**Step 2 — Clear all data:**
```bash
npx tsx scripts/clear-all-data.ts
```
This deletes in FK-safe order: ownership_records, holders, emiten, periods. Orphaned truncated holder rows are eliminated. There is no `--force` flag needed here.

**Step 3 — Re-import with fixed extractor:**
```bash
npm run import-pdf -- ./ownership_one_percent_202603.pdf
```
Do NOT use `--dry-run`. Do NOT use `--skip-validation`. The full import with Zod validation is required.

**Step 4 — Verify record count:**
```bash
npx tsx scripts/check-period.ts
```
The output must show period "2026-03" with at least 7209 ownership records.

If the import exits with an error (success: false), stop and report the error message. Do not attempt workarounds — the error must be diagnosed.

If record count is below 7209, report the actual count and the import warnings output. Do not proceed to Task 2.
  </action>
  <verify>
    <automated>cd "C:/Darwin/AI Agent/belajar/ownership" && npx tsx scripts/check-period.ts 2>&1</automated>
  </verify>
  <done>check-period.ts output shows period "2026-03" present with ownership_records count >= 7209. Import completed with success: true.</done>
</task>

<task type="auto">
  <name>Task 2: Spot-check holder name quality</name>
  <files></files>
  <action>
Run the audit script to verify holder name quality after re-import:

```bash
npx tsx scripts/audit-holder-names.ts
```

Review the output for two things:

1. **Known name spot-check section:** "Prajogo Pangestu" must appear in full. If "PRAJOGO PANGE" still appears and "PRAJOGO PANGESTU" does not, the fix from Plan 05-01 did not work correctly — stop and report.

2. **Short name candidates section:** Names shorter than 12 characters may still exist for legitimate reasons (e.g., "PT ASTRA Tbk" type abbreviations). Review them. If any obvious truncation patterns are present (name ends mid-word, or name is a partial match of a known investor), report them.

Also verify no regression in other fields by checking a sample record directly:
```bash
# Quick SQL check — run via npx tsx scripts/check-db.ts or inline query
# Verify a known stock (e.g. BBCA or TLKM) has its top holder with correct percentage
```

If check-db.ts does not support ad-hoc queries, use audit-holder-names.ts output as sufficient verification.

The audit script exit code 0 = all names >= 12 characters (fully clean). Exit code 1 = some names still short. Exit code 1 is acceptable if the short names are legitimate abbreviations — document them.
  </action>
  <verify>
    <automated>cd "C:/Darwin/AI Agent/belajar/ownership" && npx tsx scripts/audit-holder-names.ts 2>&1</automated>
  </verify>
  <done>"Prajogo Pangestu" (or its canonical normalized form PRAJOGO PANGESTU) appears in the audit output for the known-name spot-check section. No obvious mid-word truncation patterns in the short-name candidates section.</done>
</task>

</tasks>

<verification>
Phase 5 complete when ALL of the following are true:
1. `npx tsx scripts/check-period.ts` shows period "2026-03" with >= 7209 records
2. `npx tsx scripts/audit-holder-names.ts` spot-check section shows "Prajogo Pangestu" in full
3. No holder names in the database are substrings of other holder names that look like truncations
4. Both import and audit scripts exit without uncaught exceptions
</verification>

<success_criteria>
- Database contains exactly one period (2026-03) with >= 7209 ownership records
- Spot-check audit confirms known investor names are complete
- No truncated orphan holders remain (clear-all-data eliminated them)
- Import ran with Zod validation enabled and reported success: true
</success_criteria>

<output>
After completion, create `.planning/phases/05-pdf-fix-and-data-quality/05-02-SUMMARY.md` following the summary template.
</output>
